services:
  nginx:
    image: nginx:alpine

    networks:
      - chat-net
    depends_on:
      - server
    ports:
      - ${SERVER_PORT}:3000

    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - nginx-log:/var/log/nginx

volumes:
  nginx-log:
    driver: local
